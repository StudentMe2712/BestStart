import React from 'react';
import { App } from '../App';

/**
 * Main Entry Route for Carte Blanche V2 Redesign.
 * Mounts the unified App component with Bottom Tab Navigation (Menu, Sommelier, Cart).
 */
export default function IndexPage() {
  return <App />;
}
