# SYSTEM DIRECTIVE: PARSER UPGRADE ROADMAP

Ты — Senior Data Engineer & System Architect. Твоя задача — последовательно развивать систему сбора и анализа данных (Глобальный Радар).

**ЖЕСТКИЕ ПРАВИЛА ИСПОЛНЕНИЯ:**
1. Все запуски и проверки бэкенда выполняются СТРОГО через `docker compose up -d --build`. Никаких ручных запусков `uvicorn` на хосте.
2. Выполняй уровни строго по порядку. 
3. Завершив уровень, замени `[ ]` на `[x]` в этом файле, обнови `PROJECT_STATUS.md` и остановись, ожидая команды на следующий этап.

**[x] Уровень 1: Пробитие защит (Playwright & SPA)**
* Интегрировать `playwright` в зависимости бэкенда и обновить `Dockerfile` (добавить установку браузеров).
* Создать `AdvancedExtractor`, использующий headless-браузер для обхода Cloudflare и чтения сайтов с JS-рендером.
* Перевести минимум 2 источника на новый экстрактор и убедиться, что контейнер успешно парсит их и пишет в SQLite.

**[x] Уровень 2: Умная Дедупликация (Слияние трендов)**
* Внедрить механизм локального семантического сравнения (или строгой эвристики) для записей со статусом `pending`.
* Написать логику слияния: если одна и та же новость найдена на разных площадках, объединять их в один объект до отправки в Groq, повышая внутренний приоритет темы.

**[x] Уровень 3: Telegram-пылесос (Telethon)**
* Добавить библиотеку `Telethon` для асинхронного чтения Telegram.
* Настроить безопасную передачу сессий и переменных `API_ID` / `API_HASH` через `.env` файл внутрь Docker.
* Создать `TelegramExtractor` и подключить тестовые каналы, не блокируя основную очередь воркеров.

**[x] Уровень 4: Telegram Push-Алерты, Тотальный Русский & Глубокие Отчеты (Deep Reports)**
* Обновить системные промпты Groq: принудительный перевод `trend_name` и `ai_summary` на грамотный русский язык.
* Создать сервис `TelegramNotifier` для мгновенной отправки HTML Push-уведомлений о топ-трендах (`ai_score >= 9`, `scam < 15%`).
* Добавить колонку `detailed_report` в SQLite и эндпоинт `POST /api/trends/{id}/report` для генерации глубоких отчетов с планом запуска MVP за 2 недели.
* Добавить в UI модального окна интерактивную кнопку генерации и красивый рендеринг готового отчета.

**[x] Уровень 5: Лайки (Inbox Zero) & Мониторинг Источников**
* Добавить колонку `is_liked` в SQLite и эндпоинты `PATCH/PUT /api/trends/{id}/like`.
* Реализовать логику Inbox Zero: по умолчанию возвращать только непролайканные тренды (`tab=inbox` / `is_liked=0`), а по фильтру `tab=liked` — только избранное.
* Легализовать Telegram-каналы в таблице `sources` и отдавать полный список через `GET /api/sources`.
* Внедрить на фронтенде сердечко с Optimistic UI, вкладку «Избранное» и модалку «Мониторинг источников» с группировкой по типам.