<Application x:Class="NexusCommander.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:NexusCommander.Converters"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Windows 11 Dark Mode Semi-Translucent Mica Glassmorphism Color Palette -->
        <Color x:Key="ColorBackground">#B0141414</Color>       <!-- Main List Area Background: #B0141414 -->
        <Color x:Key="ColorMica">#80202020</Color>             <!-- Top / Header / Status Bar: #80202020 -->
        <Color x:Key="ColorMicaSecondary">#901C1C1C</Color>    <!-- Secondary Mica Surface: #901C1C1C -->
        <Color x:Key="ColorSidebar">#90181818</Color>          <!-- Sidebar Background: #90181818 -->
        <Color x:Key="ColorSurface">#452D2D2D</Color>          <!-- Input & Card Surface -->
        <Color x:Key="ColorSurfaceHover">#60383838</Color>
        <Color x:Key="ColorSurfacePressed">#70303030</Color>
        <Color x:Key="ColorBorder">#30FFFFFF</Color>           <!-- Dividers & Separators: #30FFFFFF -->
        <Color x:Key="ColorBorderLight">#20FFFFFF</Color>      <!-- Subtle Outer Borders: #20FFFFFF -->
        <Color x:Key="ColorBorderSubtle">#15FFFFFF</Color>
        <Color x:Key="ColorAccent">#0078D4</Color>
        <Color x:Key="ColorAccentLight">#4CC2FF</Color>
        <Color x:Key="ColorAccentDark">#005A9E</Color>
        <Color x:Key="ColorAccentHover">#60CDFF</Color>
        <Color x:Key="ColorTextPrimary">#FFFFFF</Color>
        <Color x:Key="ColorTextSecondary">#D0D0D0</Color>
        <Color x:Key="ColorTextMuted">#8A8A8A</Color>
        <Color x:Key="ColorFolder">#F5C842</Color>
        <Color x:Key="ColorFile">#F0F0F0</Color>
        <Color x:Key="ColorItemHover">#15FFFFFF</Color>
        <Color x:Key="ColorItemSelected">#25FFFFFF</Color>
        <Color x:Key="ColorItemSelectedInactive">#18FFFFFF</Color>
        <Color x:Key="ColorError">#FF99A4</Color>
        <Color x:Key="ColorErrorBg">#4A1E24</Color>
        <Color x:Key="ColorSuccess">#6CCB5F</Color>

        <!-- SolidColorBrushes -->
        <SolidColorBrush x:Key="BrushBackground" Color="{StaticResource ColorBackground}" />
        <SolidColorBrush x:Key="BrushMica" Color="{StaticResource ColorMica}" />
        <SolidColorBrush x:Key="BrushMicaSecondary" Color="{StaticResource ColorMicaSecondary}" />
        <SolidColorBrush x:Key="BrushSidebar" Color="{StaticResource ColorSidebar}" />
        <SolidColorBrush x:Key="BrushSecondary" Color="{StaticResource ColorMica}" />
        <SolidColorBrush x:Key="BrushSurface" Color="{StaticResource ColorSurface}" />
        <SolidColorBrush x:Key="BrushSurfaceHover" Color="{StaticResource ColorSurfaceHover}" />
        <SolidColorBrush x:Key="BrushSurfacePressed" Color="{StaticResource ColorSurfacePressed}" />
        <SolidColorBrush x:Key="BrushBorder" Color="{StaticResource ColorBorder}" />
        <SolidColorBrush x:Key="BrushBorderLight" Color="{StaticResource ColorBorderLight}" />
        <SolidColorBrush x:Key="BrushBorderSubtle" Color="{StaticResource ColorBorderSubtle}" />
        <SolidColorBrush x:Key="BrushAccent" Color="{StaticResource ColorAccent}" />
        <SolidColorBrush x:Key="BrushAccentLight" Color="{StaticResource ColorAccentLight}" />
        <SolidColorBrush x:Key="BrushAccentDark" Color="{StaticResource ColorAccentDark}" />
        <SolidColorBrush x:Key="BrushAccentHover" Color="{StaticResource ColorAccentHover}" />
        <SolidColorBrush x:Key="BrushTextPrimary" Color="{StaticResource ColorTextPrimary}" />
        <SolidColorBrush x:Key="BrushTextSecondary" Color="{StaticResource ColorTextSecondary}" />
        <SolidColorBrush x:Key="BrushTextMuted" Color="{StaticResource ColorTextMuted}" />
        <SolidColorBrush x:Key="BrushFolder" Color="{StaticResource ColorFolder}" />
        <SolidColorBrush x:Key="BrushFile" Color="{StaticResource ColorFile}" />
        <SolidColorBrush x:Key="BrushItemHover" Color="{StaticResource ColorItemHover}" />
        <SolidColorBrush x:Key="BrushItemSelected" Color="{StaticResource ColorItemSelected}" />
        <SolidColorBrush x:Key="BrushItemSelectedInactive" Color="{StaticResource ColorItemSelectedInactive}" />
        <SolidColorBrush x:Key="BrushError" Color="{StaticResource ColorError}" />
        <SolidColorBrush x:Key="BrushErrorBg" Color="{StaticResource ColorErrorBg}" />
        <SolidColorBrush x:Key="BrushSuccess" Color="{StaticResource ColorSuccess}" />

        <!-- Converters -->
        <converters:BoolToVisibilityConverter x:Key="BoolToVis" />
        <converters:BoolToVisibilityConverter x:Key="InverseBoolToVis" Invert="True" />
        <converters:StringNullOrEmptyToVisibilityConverter x:Key="StringNullOrEmptyToVis" />
        <converters:ItemTypeToBrushConverter x:Key="ItemTypeToBrush" />
        <converters:SidebarActiveBrushConverter x:Key="SidebarActiveBrushConv" />
        <converters:SidebarIndicatorVisibilityConverter x:Key="SidebarIndicatorVisConv" />
        <converters:SortColumnToGlyphConverter x:Key="SortColumnToGlyph" />

        <!-- Microsoft Fluent UI System Icons 20 Regular (High-Precision Vector StreamGeometry Resources) -->
        <StreamGeometry x:Key="GeoArrowLeft">M9.15898 16.8666C9.36292 17.0528 9.67918 17.0384 9.86536 16.8345C10.0515 16.6305 10.0371 16.3143 9.8332 16.1281L3.66535 10.4974H17.4961C17.7722 10.4974 17.9961 10.2735 17.9961 9.99736C17.9961 9.72122 17.7722 9.49736 17.4961 9.49736H3.66824L9.8332 3.86927C10.0371 3.68309 10.0515 3.36684 9.86536 3.16289C9.67918 2.95895 9.36292 2.94458 9.15898 3.13076L1.83852 9.80004C1.72895 9.89988 1.66667 10.0427 1.66667 10.1925C1.66667 10.3423 1.72895 10.4851 1.83852 10.5849L9.15898 16.8666Z</StreamGeometry>
        <StreamGeometry x:Key="GeoArrowRight">M10.841 16.8666C10.6371 17.0528 10.3208 17.0384 10.1346 16.8345C9.94846 16.6305 9.96283 16.3143 10.1668 16.1281L16.3347 10.4974H2.50391C2.22776 10.4974 2.00391 10.2735 2.00391 9.99736C2.00391 9.72122 2.22776 9.49736 2.50391 9.49736H16.3318L10.1668 3.86927C9.96283 3.68309 9.94846 3.36684 10.1346 3.16289C10.3208 2.95895 10.6371 2.94458 10.841 3.13076L18.1615 9.80004C18.271 9.89988 18.3333 10.0427 18.3333 10.1925C18.3333 10.3423 18.271 10.4851 18.1615 10.5849L10.841 16.8666Z</StreamGeometry>
        <StreamGeometry x:Key="GeoArrowUp">M9.8332 3.66535L4.20257 9.8332C4.01639 10.0371 3.70014 10.0515 3.49619 9.86536C3.29225 9.67918 3.27788 9.36292 3.46406 9.15898L10.1333 1.83852C10.2332 1.72895 10.376 1.66667 10.5258 1.66667C10.6756 1.66667 10.8184 1.72895 10.9183 1.83852L17.5875 9.15898C17.7737 9.36292 17.7593 9.67918 17.5554 9.86536C17.3514 10.0515 17.0352 10.0371 16.849 9.8332L11.2184 3.66535V17.4961C11.2184 17.7722 10.9945 17.9961 10.7184 17.9961C10.4422 17.9961 10.2184 17.7722 10.2184 17.4961V3.66535H9.8332Z</StreamGeometry>
        <StreamGeometry x:Key="GeoRefresh">M10 2.5C5.85786 2.5 2.5 5.85786 2.5 10C2.5 14.1421 5.85786 17.5 10 17.5C13.626 17.5 16.6577 14.9317 17.3392 11.5168C17.3934 11.2452 17.2184 10.9806 16.9468 10.9264C16.6752 10.8722 16.4106 11.0472 16.3564 11.3188C15.7899 14.1589 13.2687 16.25 10 16.25C6.54822 16.25 3.75 13.4518 3.75 10C3.75 6.54822 6.54822 3.75 10 3.75C11.8344 3.75 13.4735 4.54019 14.6074 5.80718L13.125 5.80718C12.7798 5.80718 12.5 6.08698 12.5 6.43218C12.5 6.77738 12.7798 7.05718 13.125 7.05718H16.25C16.5952 7.05718 16.875 6.77738 16.875 6.43218V3.30718C16.875 2.96198 16.5952 2.68218 16.25 2.68218C15.9048 2.68218 15.625 2.96198 15.625 3.30718V4.76785C14.2483 3.35987 12.2343 2.5 10 2.5Z</StreamGeometry>
        <StreamGeometry x:Key="GeoSearch">M8.5 2C4.91015 2 2 4.91015 2 8.5C2 12.0899 4.91015 15 8.5 15C10.024 15 11.4239 14.4754 12.5312 13.5919L16.4697 17.5303C16.7626 17.8232 17.2374 17.8232 17.5303 17.5303C17.8232 17.2374 17.8232 16.7626 17.5303 16.4697L13.5919 12.5312C14.4754 11.4239 15 10.024 15 8.5C15 4.91015 12.0899 2 8.5 2ZM3.25 8.5C3.25 5.5995 5.5995 3.25 8.5 3.25C11.4005 3.25 13.75 5.5995 13.75 8.5C13.75 11.4005 11.4005 13.75 8.5 13.75C5.5995 13.75 3.25 11.4005 3.25 8.5Z</StreamGeometry>
        <StreamGeometry x:Key="GeoCut">M6.41421 13.5858C5.63316 12.8047 4.36683 12.8047 3.58579 13.5858C2.80474 14.3668 2.80474 15.6332 3.58579 16.4142C4.36683 17.1953 5.63316 17.1953 6.41421 16.4142C7.03715 15.7913 7.18556 14.8872 6.85945 14.1167L10 10.9761L13.1405 14.1167C12.8144 14.8872 12.9629 15.7913 13.5858 16.4142C14.3668 17.1953 15.6332 17.1953 16.4142 16.4142C17.1953 15.6332 17.1953 14.3668 16.4142 13.5858C15.6332 12.8047 14.3668 12.8047 13.5858 13.5858C12.9629 14.2087 12.8144 15.1128 13.1405 15.8833L10 12.7428L6.85945 15.8833C7.18556 15.1128 7.03715 14.2087 6.41421 13.5858ZM10 9.25L14.7071 4.54289C14.8946 4.35536 15.1979 4.35536 15.3854 4.54289C15.573 4.73043 15.573 5.03374 15.3854 5.22127L10.7071 9.89962L10 9.25ZM9.29289 9.89962L4.61457 5.22127C4.42704 5.03374 4.42704 4.73043 4.61457 4.54289C4.80211 4.35536 5.10542 4.35536 5.29295 4.54289L10 9.25L9.29289 9.89962Z</StreamGeometry>
        <StreamGeometry x:Key="GeoCopy">M4 3C2.89543 3 2 3.89543 2 5V13C2 14.1046 2.89543 15 4 15H5V16C5 17.1046 5.89543 18 7 18H15C16.1046 18 17 17.1046 17 16V8C17 6.89543 16.1046 6 15 6H14V5C14 3.89543 13.1046 3 12 3H4ZM4 4.25H12C12.4142 4.25 12.75 4.58579 12.75 5V6H7C5.89543 6 5 6.89543 5 8V13.75H4C3.58579 13.75 3.25 13.4142 3.25 13V5C3.25 4.58579 3.58579 4.25 4 4.25ZM6.25 8C6.25 7.58579 6.58579 7.25 7 7.25H15C15.4142 7.25 15.75 7.58579 15.75 8V16C15.75 16.4142 15.4142 16.75 15 16.75H7C6.58579 16.75 6.25 16.4142 6.25 16V8Z</StreamGeometry>
        <StreamGeometry x:Key="GeoPaste">M8.5 2C7.39543 2 6.5 2.89543 6.5 4H4C2.89543 4 2 4.89543 2 6V15C2 16.1046 2.89543 17 4 17H16C17.1046 17 18 16.1046 18 15V6C18 4.89543 17.1046 4 16 4H13.5C13.5 2.89543 12.6046 2 11.5 2H8.5ZM7.75 4C7.75 3.58579 8.08579 3.25 8.5 3.25H11.5C11.9142 3.25 12.25 3.58579 12.25 4H7.75ZM3.25 6C3.25 5.58579 3.58579 5.25 4 5.25H5.25V6.5C5.25 7.05228 5.69772 7.5 6.25 7.5H13.75C14.3023 7.5 14.75 7.05228 14.75 6.5V5.25H16C16.4142 5.25 16.75 5.58579 16.75 6V15C16.75 15.4142 16.4142 15.75 16 15.75H4C3.58579 15.75 3.25 15.4142 3.25 15V6Z</StreamGeometry>
        <StreamGeometry x:Key="GeoRename">M14.0858 2.08579C14.8668 1.30474 16.1332 1.30474 16.9142 2.08579C17.6953 2.86683 17.6953 4.13317 16.9142 4.91421L6.41421 15.4142C6.15542 15.673 5.82396 15.8454 5.46261 15.9089L2.46261 16.4357C2.08588 16.502 1.7196 16.2238 1.68745 15.8427C1.68413 15.8033 1.68745 15.7637 1.69733 15.7247L2.22416 12.7247C2.28766 12.3634 2.46006 12.0319 2.71885 11.7731L13.2189 1.2731L14.0858 2.08579ZM13.5 3.5L3.60271 13.3973C3.47332 13.5267 3.38712 13.6924 3.35537 13.8731L3.02029 15.7797L4.92688 15.4446C5.10755 15.4129 5.27329 15.3267 5.40268 15.1973L15.3 5.3L13.5 3.5ZM16.0355 4.56447C16.3479 4.25205 16.3479 3.74795 16.0355 3.43553C15.7231 3.12311 15.219 3.12311 14.9066 3.43553L14.3 4.04213L15.9579 5.7L16.0355 4.56447Z</StreamGeometry>
        <StreamGeometry x:Key="GeoShare">M14.5 4C13.1193 4 12 5.11929 12 6.5C12 6.84021 12.0681 7.16452 12.1917 7.46048L7.46048 9.82609C6.96452 9.31427 6.26867 9 5.5 9C4.11929 9 3 10.1193 3 11.5C3 12.8807 4.11929 14 5.5 14C6.26867 14 6.96452 13.6857 7.46048 13.1739L12.1917 15.5395C12.0681 15.8355 12 16.1598 12 16.5C12 17.8807 13.1193 19 14.5 19C15.8807 19 17 17.8807 17 16.5C17 15.1193 15.8807 14 14.5 14C13.7313 14 13.0355 14.3143 12.5395 14.8261L7.80833 12.4605C7.93188 12.1645 8 11.8402 8 11.5C8 11.1598 7.93188 10.8355 7.80833 10.5395L12.5395 8.17391C13.0355 8.68573 13.7313 9 14.5 9C15.8807 9 17 7.88071 17 6.5C17 5.11929 15.8807 4 14.5 4Z</StreamGeometry>
        <StreamGeometry x:Key="GeoDelete">M7.5 3C7.5 2.17157 8.17157 1.5 9 1.5H11C11.8284 1.5 12.5 2.17157 12.5 3V4H16.25C16.6642 4 17 4.33579 17 4.75C17 5.16421 16.6642 5.5 16.25 5.5H15.6562L14.7774 16.0456C14.6978 17.0006 13.8996 17.75 12.9416 17.75H7.05837C6.10037 17.75 5.30219 17.0006 5.22262 16.0456L4.34375 5.5H3.75C3.33579 5.5 3 5.16421 3 4.75C3 4.33579 3.33579 4 3.75 4H7.5V3ZM8.75 4H11.25V3C11.25 2.86193 11.1381 2.75 11 2.75H9C8.86193 2.75 8.75 2.86193 8.75 3V4ZM5.59966 5.5L6.46782 15.9184C6.49434 16.2368 6.7604 16.5 7.05837 16.5H12.9416C13.2396 16.5 13.5057 16.2368 13.5322 15.9184L14.4003 5.5H5.59966ZM8 7.5C8.41421 7.5 8.75 7.83579 8.75 8.25V13.75C8.75 14.1642 8.41421 14.5 8 14.5C7.58579 14.5 7.25 14.1642 7.25 13.75V8.25C7.25 7.83579 7.58579 7.5 8 7.5ZM12 7.5C12.4142 7.5 12.75 7.83579 12.75 8.25V13.75C12.75 14.1642 12.4142 14.5 12 14.5C11.5858 14.5 11.25 14.1642 11.25 13.75V8.25C11.25 7.83579 11.5858 7.5 12 7.5Z</StreamGeometry>
        <StreamGeometry x:Key="GeoSort">M7.75 3.25C8.16421 3.25 8.5 3.58579 8.5 4V14.4393L10.9697 11.9697C11.2626 11.6768 11.7374 11.6768 12.0303 11.9697C12.3232 12.2626 12.3232 12.7374 12.0303 13.0303L8.03033 17.0303C7.73744 17.3232 7.26256 17.3232 6.96967 17.0303L2.96967 13.0303C2.67678 12.7374 2.67678 12.2626 2.96967 11.9697C3.26256 11.6768 3.73744 11.6768 4.03033 11.9697L6.5 14.4393V4C6.5 3.58579 6.83579 3.25 7.25 3.25H7.75ZM13.5 16.75C13.0858 16.75 12.75 16.4142 12.75 16V5.56066L10.2803 8.03033C9.98744 8.32322 9.51256 8.32322 9.21967 8.03033C8.92678 7.73744 8.92678 7.26256 9.21967 6.96967L13.2197 2.96967C13.5126 2.67678 13.9874 2.67678 14.2803 2.96967L18.2803 6.96967C18.5732 7.26256 18.5732 7.73744 18.2803 8.03033C17.9874 8.32322 17.5126 8.32322 17.2197 8.03033L14.75 5.56066V16C14.75 16.4142 14.4142 16.75 14 16.75H13.5Z</StreamGeometry>
        <StreamGeometry x:Key="GeoView">M3 4C3 3.44772 3.44772 3 4 3H16C16.5523 3 17 3.44772 17 4V16C17 16.5523 16.5523 17 16 17H4C3.44772 17 3 16.5523 3 16V4ZM4.5 4.5V9.25H9.25V4.5H4.5ZM10.75 4.5V9.25H15.5V4.5H10.75ZM4.5 10.75V15.5H9.25V10.75H4.5ZM10.75 10.75V15.5H15.5V10.75H10.75Z</StreamGeometry>
        <StreamGeometry x:Key="GeoAdd">M10 2.5C10.4142 2.5 10.75 2.83579 10.75 3.25V9.25H16.75C17.1642 9.25 17.5 9.58579 17.5 10C17.5 10.4142 17.1642 10.75 16.75 10.75H10.75V16.75C10.75 17.1642 10.4142 17.5 10 17.5C9.58579 17.5 9.25 17.1642 9.25 16.75V10.75H3.25C2.83579 10.75 2.5 10.4142 2.5 10C2.5 9.58579 2.83579 9.25 3.25 9.25H9.25V3.25C9.25 2.83579 9.58579 2.5 10 2.5Z</StreamGeometry>
        <StreamGeometry x:Key="GeoDismiss">M4.21967 4.21967C4.51256 3.92678 4.98744 3.92678 5.28033 4.21967L10 8.93934L14.7197 4.21967C15.0126 3.92678 15.4874 3.92678 15.7803 4.21967C16.0732 4.51256 16.0732 4.98744 15.7803 5.28033L11.0607 10L15.7803 14.7197C16.0732 15.0126 16.0732 15.4874 15.7803 15.7803C15.4874 16.0732 15.0126 16.0732 14.7197 15.7803L10 11.0607L5.28033 15.7803C4.98744 16.0732 4.51256 16.0732 4.21967 15.7803C3.92678 15.4874 3.92678 15.0126 4.21967 14.7197L8.93934 10L4.21967 5.28033C3.92678 4.98744 3.92678 4.51256 4.21967 4.21967Z</StreamGeometry>
        <StreamGeometry x:Key="GeoTerminal">M2.5 4C2.5 3.17157 3.17157 2.5 4 2.5H16C16.8284 2.5 17.5 3.17157 17.5 4V16C17.5 16.8284 16.8284 17.5 16 17.5H4C3.17157 17.5 2.5 16.8284 2.5 16V4ZM4 3.75C3.86193 3.75 3.75 3.86193 3.75 4V16C3.75 16.1381 3.86193 16.25 4 16.25H16C16.1381 16.25 16.25 16.1381 16.25 16V4C16.25 3.86193 16.1381 3.75 16 3.75H4ZM5.21967 6.46967C5.51256 6.17678 5.98744 6.17678 6.28033 6.46967L8.78033 8.96967C9.07322 9.26256 9.07322 9.73744 8.78033 10.0303L6.28033 12.5303C5.98744 12.8232 5.51256 12.8232 5.21967 12.5303C4.92678 12.2374 4.92678 11.7626 5.21967 11.4697L7.18934 9.5L5.21967 7.53033C4.92678 7.23744 4.92678 6.76256 5.21967 6.46967ZM9.5 12C9.5 11.5858 9.83579 11.25 10.25 11.25H14.25C14.6642 11.25 15 11.5858 15 12C15 12.4142 14.6642 12.75 14.25 12.75H10.25C9.83579 12.75 9.5 12.4142 9.5 12Z</StreamGeometry>
        <StreamGeometry x:Key="GeoMore">M4 10C4 9.17157 4.67157 8.5 5.5 8.5C6.32843 8.5 7 9.17157 7 10C7 10.8284 6.32843 11.5 5.5 11.5C4.67157 11.5 4 10.8284 4 10ZM8.5 10C8.5 9.17157 9.17157 8.5 10 8.5C10.8284 8.5 11.5 9.17157 11.5 10C11.5 10.8284 10.8284 11.5 10 11.5C9.17157 11.5 8.5 10.8284 8.5 10ZM13 10C13 9.17157 13.6716 8.5 14.5 8.5C15.3284 8.5 16 9.17157 16 10C16 10.8284 15.3284 11.5 14.5 11.5C13.6716 11.5 13 10.8284 13 10Z</StreamGeometry>

        <!-- Base Windows 11 Typography -->
        <FontFamily x:Key="AppFont">Segoe UI Variable Display, Segoe UI Variable Text, Segoe UI, -apple-system, BlinkMacSystemFont, Roboto, sans-serif</FontFamily>
        <FontFamily x:Key="MonoFont">Cascadia Code, Consolas, Segoe UI Mono, monospace</FontFamily>
        <FontFamily x:Key="IconFont">Segoe UI Emoji, Segoe Fluent Icons, Segoe MDL2 Assets, Segoe UI Symbol</FontFamily>

        <!-- Modern Sleek ScrollBar Style -->
        <Style x:Key="ScrollBarThumbStyle" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Background" Value="#484848" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="ThumbBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                SnapsToDevicePixels="True" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="#666666" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="{StaticResource BrushAccentLight}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Width" Value="6" />
                    <Setter Property="Height" Value="Auto" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                <Grid Background="Transparent">
                                    <Track x:Name="PART_Track" IsDirectionReversed="True">
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ScrollBarThumbStyle}" />
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Height" Value="6" />
                    <Setter Property="Width" Value="Auto" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                <Grid Background="Transparent">
                                    <Track x:Name="PART_Track">
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ScrollBarThumbStyle}" />
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- GridViewColumnHeader Style (Windows 11 Minimalist Flat Header) -->
        <Style TargetType="{x:Type GridViewColumnHeader}">
            <Setter Property="Background" Value="{StaticResource BrushBackground}" />
            <Setter Property="Foreground" Value="{StaticResource BrushTextSecondary}" />
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                        <Grid SnapsToDevicePixels="True">
                            <Border x:Name="HeaderBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="HeaderBorder" Property="Background" Value="#12FFFFFF" />
                                <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="HeaderBorder" Property="Background" Value="#1EFFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListViewItem Style (Windows 11 Fluent Row ~36px) -->
        <Style TargetType="{x:Type ListViewItem}">
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="4,2,4,2" />
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="MinHeight" Value="36" />
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListViewItem}">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <GridViewRowPresenter VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Hover -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#0FFFFFFF" />
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#15FFFFFF" />
                            </Trigger>
                            <!-- Selected Active / Focused -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#18FFFFFF" />
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#25FFFFFF" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                            <!-- Selected Inactive -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True" />
                                    <Condition Property="Selector.IsSelectionActive" Value="False" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="ItemBorder" Property="Background" Value="#12FFFFFF" />
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="Transparent" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ListView Style -->
        <Style TargetType="{x:Type ListView}">
            <Setter Property="Background" Value="{StaticResource BrushBackground}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
            <Setter Property="VirtualizingStackPanel.IsVirtualizing" Value="True" />
            <Setter Property="VirtualizingStackPanel.VirtualizationMode" Value="Recycling" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>

        <!-- Modern TextBox Style (Win11 Pill Input) -->
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Background" Value="#2D2D2D" />
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
            <Setter Property="CaretBrush" Value="{StaticResource BrushAccentLight}" />
            <Setter Property="SelectionBrush" Value="{StaticResource BrushAccent}" />
            <Setter Property="BorderBrush" Value="#383838" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Height" Value="32" />
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"
                                          VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#484848" />
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource BrushAccentLight}" />
                                <Setter TargetName="border" Property="Background" Value="#2D2D2D" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Windows 11 Input Container Border Style (for Address Bar & Search Box) -->
        <Style x:Key="Win11InputContainerStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="#2D2D2D" />
            <Setter Property="BorderBrush" Value="#383838" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#484848" />
                </Trigger>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource BrushAccentLight}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Standard Button Style -->
        <Style TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{StaticResource BrushSurface}" />
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorderLight}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="btnBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="{StaticResource BrushSurfaceHover}" />
                                <Setter TargetName="btnBorder" Property="BorderBrush" Value="#4E4E4E" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="{StaticResource BrushSurfacePressed}" />
                                <Setter TargetName="btnBorder" Property="BorderBrush" Value="{StaticResource BrushAccent}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="btnBorder" Property="Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Nav Icon Button Style (Circular / Rounded Frame: Back/Forward/Up/Refresh/Tabs) -->
        <Style x:Key="NavIconButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Width" Value="32" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="1,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="navBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="navBorder" Property="Background" Value="#1AFFFFFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="navBorder" Property="Background" Value="#25FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}" />
                                <Setter TargetName="navBorder" Property="Opacity" Value="0.35" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Windows 11 Command Bar Text Button Style -->
        <Style x:Key="CommandBarButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Margin" Value="2,0" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="cmdBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="cmdBorder" Property="Background" Value="#1AFFFFFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="cmdBorder" Property="Background" Value="#25FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}" />
                                <Setter TargetName="cmdBorder" Property="Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Windows 11 Command Bar Action Icon Button (Compact: Cut, Copy, Paste, Rename, Share, Delete) -->
        <Style x:Key="CommandBarActionButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Width" Value="36" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="1,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="actBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="actBorder" Property="Background" Value="#1AFFFFFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="actBorder" Property="Background" Value="#25FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}" />
                                <Setter TargetName="actBorder" Property="Opacity" Value="0.35" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Breadcrumb Item Button Style -->
        <Style x:Key="BreadcrumbButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="Height" Value="24" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="crumbBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="crumbBorder" Property="Background" Value="#1AFFFFFF" />
                                <Setter Property="Foreground" Value="{StaticResource BrushAccentLight}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="crumbBorder" Property="Background" Value="#25FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Windows 11 TreeView Chevron Expander ToggleButton Style (16x16 crisp vector arrow) -->
        <Style x:Key="Win11TreeViewExpandCollapseToggleStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Width" Value="16" />
            <Setter Property="Height" Value="16" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0,0,2,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border x:Name="toggleBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                Width="16" Height="16"
                                SnapsToDevicePixels="True">
                            <Path x:Name="arrowPath"
                                  Data="M6 4L10 8L6 12"
                                  Stroke="{StaticResource BrushTextMuted}"
                                  StrokeThickness="1.5"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  SnapsToDevicePixels="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="toggleBorder" Property="Background" Value="#15FFFFFF" />
                                <Setter TargetName="arrowPath" Property="Stroke" Value="{StaticResource BrushTextPrimary}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="arrowPath" Property="Data" Value="M4 6L8 10L12 6" />
                                <Setter TargetName="arrowPath" Property="Stroke" Value="{StaticResource BrushTextSecondary}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Windows 11 Fluent TreeViewItem Style -->
        <Style x:Key="Win11TreeViewItemStyle" TargetType="{x:Type TreeViewItem}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TreeViewItem}">
                        <Grid SnapsToDevicePixels="True">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!-- Item Row Wrapper -->
                            <Border x:Name="ItemBorder"
                                    Grid.Row="0"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="Transparent"
                                    BorderThickness="0"
                                    Margin="4,1,4,1"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="4"
                                    SnapsToDevicePixels="True">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Chevron ToggleButton (Hidden when no children) -->
                                    <ToggleButton x:Name="Expander"
                                                  Grid.Column="0"
                                                  Style="{StaticResource Win11TreeViewExpandCollapseToggleStyle}"
                                                  IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                                  ClickMode="Press"
                                                  Focusable="False"
                                                  VerticalAlignment="Center" />

                                    <ContentPresenter x:Name="PART_Header"
                                                      Grid.Column="1"
                                                      ContentSource="Header"
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                </Grid>
                            </Border>

                            <!-- Child Items Presenter with 24px left indentation -->
                            <ItemsPresenter x:Name="ItemsHost"
                                            Grid.Row="1"
                                            Margin="24,0,0,0"
                                            Visibility="Collapsed" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden" />
                            </Trigger>
                            <DataTrigger Binding="{Binding HasChildren}" Value="False">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden" />
                            </DataTrigger>

                            <!-- Hover state (#12FFFFFF) -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#12FFFFFF" />
                            </Trigger>

                            <!-- Selected / Active state (#1EFFFFFF) -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#1EFFFFFF" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>

                            <!-- ViewModel IsActive State (#1EFFFFFF) -->
                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#1EFFFFFF" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </DataTrigger>

                            <!-- Separator rendering: transparent, zero padding, non-clickable -->
                            <DataTrigger Binding="{Binding IsSeparator}" Value="True">
                                <Setter TargetName="Expander" Property="Visibility" Value="Collapsed" />
                                <Setter TargetName="ItemBorder" Property="Background" Value="Transparent" />
                                <Setter TargetName="ItemBorder" Property="Padding" Value="0" />
                                <Setter TargetName="ItemBorder" Property="Margin" Value="0" />
                                <Setter Property="Focusable" Value="False" />
                                <Setter Property="Cursor" Value="Arrow" />
                                <Setter Property="IsHitTestVisible" Value="False" />
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSeparator}" Value="True">
                    <Setter Property="Focusable" Value="False" />
                    <Setter Property="IsHitTestVisible" Value="False" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Windows 11 Fluent TreeView Style -->
        <Style x:Key="Win11TreeViewStyle" TargetType="{x:Type TreeView}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="6,6" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource Win11TreeViewItemStyle}" />
        </Style>

        <!-- ContextMenu Top Action Icon Button Style -->
        <Style x:Key="ContextMenuTopActionButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Width" Value="36" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Margin" Value="2,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="topActBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="topActBorder" Property="Background" Value="#1AFFFFFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="topActBorder" Property="Background" Value="#25FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="topActBorder" Property="Opacity" Value="0.35" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Windows 11 Context Menu Style -->
        <Style TargetType="{x:Type ContextMenu}">
            <Setter Property="Background" Value="#2C2C2C" />
            <Setter Property="BorderBrush" Value="{StaticResource BrushBorderLight}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
            <Setter Property="Padding" Value="4,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" BlurRadius="16" ShadowDepth="4" Opacity="0.65" />
                            </Border.Effect>
                            <StackPanel IsItemsHost="True" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Windows 11 MenuItem Style -->
        <Style TargetType="{x:Type MenuItem}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Border x:Name="menuBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"
                                Margin="2,1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="22" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0" ContentSource="Icon" VerticalAlignment="Center" />
                                <ContentPresenter Grid.Column="1" ContentSource="Header" RecognizesAccessKey="True" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="2" Text="{TemplateBinding InputGestureText}" Foreground="{StaticResource BrushTextMuted}" FontSize="11" VerticalAlignment="Center" Margin="16,0,0,0" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="menuBorder" Property="Background" Value="#1AFFFFFF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Windows 11 Separator Style -->
        <Style TargetType="{x:Type Separator}">
            <Setter Property="Background" Value="{StaticResource BrushBorder}" />
            <Setter Property="Height" Value="1" />
            <Setter Property="Margin" Value="4,4" />
        </Style>
    </Application.Resources>
</Application>