"""Echo Character Evaluation Suite.

Not a code test — a *character* benchmark. It runs Echo through life scenarios and
scores how human / curious / on-Bible the replies are, with a deterministic phrase-penalty
layer (reuses app.quality) and an optional LLM-as-judge layer. See character.md for the spec.
"""
